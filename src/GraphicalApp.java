import javax.swing.*;
import javax.swing.table.DefaultTableModel;

public class GraphicalApp {
    static DefaultTableModel Model; // to store and manage the data displayed in a JTable
    static JTable Table; // displays data in a tabular format (rows and columns)
    static JFrame Frame; // application's window with a title bar and border
    protected static final Storage storage = new Storage(); // one load
}